BevelDesk — a Windows 95-style desktop, hand-drawn in Dear ImGui.
This is a browser demo running as WebAssembly.

The filesystem here is a small in-memory mock. On the native build,
Explorer browses your REAL disk and the MS-DOS Prompt runs a real shell.

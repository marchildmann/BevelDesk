// Proof-of-concept: a classic Macintosh System 1 desktop, rendered with the
// same ImDrawList approach as the Win95 shell — the theme-seam demo (issue #1).
#pragma once

struct AppState;
void DrawMacDesktop(AppState& app);
